# Backend environment variables example
# MongoDB connection (without database name)
MONGODB_URI=mongodb://localhost:27017
# Application port
PORT=5000
# CORS allowed origin (frontend URL)
CORS_ORIGIN=http://localhost:5173
# JWT secrets (replace with strong random strings in production)
ACCESS_TOKEN_SECRET=replace-with-strong-access-token-secret
REFRESH_TOKEN_SECRET=replace-with-strong-refresh-token-secret
# Token expiry values (e.g. '15m', '7d')
ACCESS_TOKEN_EXPIRY=15m
REFRESH_TOKEN_EXPIRY=7d
# Node environment
NODE_ENV=development
