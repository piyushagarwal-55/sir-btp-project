# Frontend environment variables example (Vite - must start with VITE_)
VITE_API_URL=http://localhost:5000/api
# You can use this to control where auth redirects back to after login
VITE_AUTH_REDIRECT=http://localhost:5173
